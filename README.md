# Rule-Based-AI-Chatbot
It interacts with users in a continuous loop, responds to common greetings and questions based on predefined rules (using a dictionary), and gracefully handles unknown inputs with a fallback message. User inputs are sanitized for case and extra spaces. The chatbot includes a clean exit mechanism through commands like 'bye' or 'exit'.
